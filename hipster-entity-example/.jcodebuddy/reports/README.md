# `reports/` — run records worth keeping

**Tracked.** Human-read records of generator runs that are evidence rather than noise:
gate results, baseline readings, regeneration diffs, divergence summaries.

The test is whether a future reader would want it: a record that justifies a decision
belongs here, a raw tool log does not (that is `agent-state/`).
