# `context/` — module-scoped generator context

**Tracked.** Hand-written material that belongs to *this module's* generators: specs,
invariants, naming contracts, examples of acceptable generated output.

This is deliberately not a home for repository-wide planning or research documents —
those belong to the repository, not to a module that happens to use JCodeBuddy.
