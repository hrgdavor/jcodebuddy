# `metadata/project/` — whole-project metadata

Whole-project metadata passes: `index.fury` and related index files that describe the
module as a whole rather than one entity. Reserved by the layout so a project-wide pass
has a defined home instead of inventing a new root directory.

Ignored by default — see `../README.md`.
